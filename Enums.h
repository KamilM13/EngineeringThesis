#ifndef ENUMS_H
#define ENUMS_H

typedef enum msgID
{

} msgID;

typedef enum Messages
{
    sigSpeed, // 0x13
    sigDistanceWarning, // 0x12

    totalSignals
} messages;

#endif // ENUMS_H
